# house-prices-ml-pipeline-1
End-to-end machine learning pipeline using scikit-learn

+End-to-end machine learning pipeline using scikit-learn.
+
+## Setup
+Install the required dependencies before running the training script:
+
+```bash
+pip install -r requirements.txt
+```
+
+## Training
+Run the model training pipeline from the project root:
+
+```bash
+python -m src.train
+```

